{% extends 'base.html.twig' %}


{% block title %}
    <title>quete twig</title>
{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1>Ma quête Twig-vetements</h1>
            </div>
        </div>
         <div class="row">
             {% for product in products %}
                 <div class="col-lg-4">
                      {{ product }}
                 </div>
             {% endfor %}
         </div>
        <div class="row">
            {% for vetement in vetements %}
                <div class="col-lg-6 text-center mt-4 pt-2">
                    {{ vetement }}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
